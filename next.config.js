/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js 15 handles server actions by default, but this ensures compatibility
  reactStrictMode: true,
}

module.exports = nextConfig
